set PATH ~/bin $PATH

source (dirname (status -f))/config/aliases.fish
source (dirname (status -f))/config/git-aliases.fish
source (dirname (status -f))/config/sbt.fish
source (dirname (status -f))/config/hub.fish
